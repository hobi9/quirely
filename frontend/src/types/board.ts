export type BoardCreation = {
  title: string;
  imageUrl: string;
};

export type BoardImage = { id: string; url: string };

export type Board = {
  id: number;
  title: string;
  imageUrl: string;
};
