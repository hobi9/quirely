export type BoardCreation = {
  title: string;
  thumbnailUrl: string;
  fullUrl: string;
};

export type BoardImage = {
  id: string;
  thumbnailUrl: string;
  fullUrl: string;
  description: string;
};

export type Board = {
  id: number;
  title: string;
  thumbnailUrl: string;
  fullUrl: string;
};
